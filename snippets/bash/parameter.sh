# "${x:-}"              # x が未定義でも空文字として扱う
# "${x:-default}"       # x が空なら default
# "${path##*/}"         # パスからファイル名だけ取る
# "${path%/*}"          # パスからディレクトリだけ取る
# "${file%.txt}"        # 末尾の .txt を削る
# "${file%.fastq.gz}"   # 末尾の .fastq.gz を削る
# "${r1/_R1/_R2}"       # 最初の _R1 を _R2 に置換
# "${x// /_}"           # スペースを全部 _ に置換
# "${#x}"               # 文字列長
# "${x:0:3}"            # 先頭3文字
# "${x: -3}"            # 末尾3文字


file="${path##*/}"
sample="${file%.fastq.gz}"
r2="${r1/_R1/_R2}"
editor="${EDITOR:-vi}"

